---
# k8s/secret-template.yaml
apiVersion: v1
kind: Secret
metadata:
  name: recipe-scraper-secrets
  namespace: recipe-scraper
type: Opaque
stringData:
  RECIPE_SCRAPER_DB_PASSWORD: "${RECIPE_SCRAPER_DB_PASSWORD}"   # Injected during deployment
  SPOONACULAR_API_KEY: "${SPOONACULAR_API_KEY}"   # Injected during deployment
