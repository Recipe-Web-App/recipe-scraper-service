# =============================================================================
# Recipe Scraper Service - Environment Configuration
# =============================================================================
# This file contains SECRETS ONLY. All other configuration lives in YAML files
# under the config/ directory.
#
# Copy this file to .env and update values for your environment.
# NEVER commit .env files to version control!

# -----------------------------------------------------------------------------
# Environment Selection
# -----------------------------------------------------------------------------
# Determines which config/environments/{APP_ENV}/ files are loaded.
# Options: local, test, development, staging, production
APP_ENV=development

# -----------------------------------------------------------------------------
# Secrets
# -----------------------------------------------------------------------------
# JWT secret key for token signing/verification
# Generate with: python -c "import secrets; print(secrets.token_urlsafe(32))"
JWT_SECRET_KEY=

# Redis password (leave empty if no auth required)
REDIS_PASSWORD=

# OAuth2 client secret for auth service integration (introspection mode)
AUTH_SERVICE_CLIENT_SECRET=

# Sentry DSN for error tracking (optional)
SENTRY_DSN=

# Service API keys for service-to-service auth (comma-separated)
SERVICE_API_KEYS=
