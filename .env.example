# =============================================================================
# Recipe Scraper Service - Environment Configuration
# =============================================================================
# This file is a TEMPLATE - copy it to create your actual env files.
# NEVER commit actual .env files to version control!
#
# ENV FILE LOCATIONS:
# -------------------
# Local development:
#   Copy to: .env.local (project root)
#   Usage:   Used when running the app locally
#
# Kubernetes deployments:
#   Copy to: k8s/overlays/{environment}/.env
#   Examples:
#     - k8s/overlays/development/.env
#     - k8s/overlays/staging/.env
#     - k8s/overlays/production/.env
#   Usage:   Kustomize reads these to generate K8s secrets
#
# Non-secret configuration lives in config/**/*.yaml files.
# =============================================================================

# -----------------------------------------------------------------------------
# Environment Selection
# -----------------------------------------------------------------------------
# Determines which config/environments/{APP_ENV}/ files are loaded.
# Options: local, test, development, staging, production
APP_ENV=local

# -----------------------------------------------------------------------------
# Secrets
# -----------------------------------------------------------------------------

# JWT Configuration
# Generate with: python -c "import secrets; print(secrets.token_urlsafe(32))"
JWT_SECRET_KEY=
JWT_ALGORITHM=HS256

# Redis password (leave empty if no auth required)
# Host/port configured in config/base/redis.yaml
REDIS_PASSWORD=

# OAuth2 Client Credentials (if using external auth service / introspection mode)
AUTH_SERVICE_CLIENT_SECRET=

# LLM - Groq API Key (for fallback when Ollama unavailable)
# Get your key from: https://console.groq.com
GROQ_API_KEY=

# Observability - Sentry DSN (optional)
SENTRY_DSN=

# Service API Keys (comma-separated, for service-to-service auth)
SERVICE_API_KEYS=
